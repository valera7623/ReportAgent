{
  "name": "reportagent-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "ReportAgent SPA (Vanilla JS). No npm build — served statically by FastAPI at /app/",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "generate-seo-assets": "python3 ../scripts/generate_seo_assets.py",
    "inject-seo": "bash ../scripts/inject-frontend-seo.sh"
  }
}
