{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (endpoint) (rate(report_requests_total[1m]))",
          "legendFormat": "{{endpoint}}",
          "refId": "A"
        }
      ],
      "title": "RPS by endpoint (last hour)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum by (le) (rate(report_generation_duration_seconds_bucket[5m])))",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.95, sum by (le) (rate(report_generation_duration_seconds_bucket[5m])))",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "expr": "histogram_quantile(0.99, sum by (le) (rate(report_generation_duration_seconds_bucket[5m])))",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "title": "Report generation duration (p50/p95/p99)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (agent_name, le) (rate(agent_duration_seconds_bucket[5m])))",
          "legendFormat": "{{agent_name}}",
          "refId": "A"
        }
      ],
      "title": "Agent duration p95",
      "type": "barchart"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum by (agent_name, error_type) (increase(agent_errors_total[1h]))",
          "format": "table",
          "instant": true,
          "refId": "A"
        }
      ],
      "title": "Agent errors (last hour)",
      "type": "table"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 16 },
      "id": 5,
      "targets": [
        {
          "expr": "celery_queue_length",
          "legendFormat": "queue",
          "refId": "A"
        }
      ],
      "title": "Celery queue length",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 4, "x": 8, "y": 16 },
      "id": 6,
      "targets": [
        {
          "expr": "active_users",
          "refId": "A"
        }
      ],
      "title": "Active users",
      "type": "gauge"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 12, "y": 16 },
      "id": 7,
      "targets": [
        {
          "expr": "sum by (status) (rate(voice_transcriptions_total[5m]))",
          "legendFormat": "{{status}}",
          "refId": "A"
        }
      ],
      "title": "Voice transcriptions (success/fail)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 22 },
      "id": 8,
      "targets": [
        {
          "expr": "rate(container_cpu_usage_seconds_total{name=~\"reportagent.*\"}[5m])",
          "legendFormat": "{{name}} CPU",
          "refId": "A"
        }
      ],
      "title": "Container CPU (cAdvisor)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "bytes" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 22 },
      "id": 9,
      "targets": [
        {
          "expr": "container_memory_usage_bytes{name=~\"reportagent.*\"}",
          "legendFormat": "{{name}} RAM",
          "refId": "A"
        }
      ],
      "title": "Container memory (cAdvisor)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 30 },
      "id": 10,
      "targets": [
        {
          "expr": "topk(10, histogram_quantile(0.95, sum by (agent_name, le) (rate(agent_duration_seconds_bucket[1h]))))",
          "format": "table",
          "instant": true,
          "refId": "A"
        }
      ],
      "title": "Top 10 slowest agents (p95)",
      "type": "table"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 38 },
      "id": 11,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (output_format, status) (rate(report_format_requests_total[5m]))",
          "legendFormat": "{{output_format}} ({{status}})",
          "refId": "A"
        }
      ],
      "title": "Форматы вывода (requests/sec)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 46 },
      "id": 12,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (output_format, le) (rate(report_format_duration_seconds_bucket[5m])))",
          "legendFormat": "{{output_format}} p95",
          "refId": "A"
        }
      ],
      "title": "Format generation duration p95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 46 },
      "id": 13,
      "targets": [
        {
          "expr": "increase(notion_api_errors_total[1h])",
          "legendFormat": "Notion errors",
          "refId": "A"
        },
        {
          "expr": "increase(google_slides_api_errors_total[1h])",
          "legendFormat": "Google Slides errors",
          "refId": "B"
        }
      ],
      "title": "External API errors (Notion / Google Slides)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "max": 1, "min": 0 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 54 },
      "id": 14,
      "targets": [
        {
          "expr": "sum(rate(self_healing_attempts_total{success=\"true\"}[1h])) / sum(rate(self_healing_attempts_total[1h]))",
          "legendFormat": "success rate",
          "refId": "A"
        }
      ],
      "title": "Self-healing success rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 54 },
      "id": 15,
      "targets": [
        {
          "expr": "topk(5, sum by (agent_name) (increase(self_healing_attempts_total[24h])))",
          "format": "table",
          "instant": true,
          "refId": "A"
        }
      ],
      "title": "Top 5 self-healing errors by agent",
      "type": "table"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 62 },
      "id": 16,
      "targets": [
        {
          "expr": "knowledge_base_size",
          "refId": "A"
        }
      ],
      "title": "Knowledge base size (ChromaDB)",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 6, "w": 16, "x": 8, "y": 62 },
      "id": 17,
      "targets": [
        {
          "expr": "sum by (source) (increase(self_healing_fixes_applied_total[24h]))",
          "legendFormat": "{{source}}",
          "refId": "A"
        }
      ],
      "title": "Fixes applied (auto vs manual)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "max": 1, "min": 0 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 68 },
      "id": 18,
      "targets": [
        {
          "expr": "sum(rate(webhook_attempts_total{success=\"true\"}[1h])) / sum(rate(webhook_attempts_total[1h]))",
          "legendFormat": "delivery success rate",
          "refId": "A"
        }
      ],
      "title": "Webhook delivery rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 68 },
      "id": 19,
      "targets": [
        {
          "expr": "sum by (event) (rate(webhook_attempts_total[5m]))",
          "legendFormat": "{{event}}",
          "refId": "A"
        }
      ],
      "title": "Webhook attempts by event",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["reportagent", "observability"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "utc",
  "title": "ReportAgent Main",
  "uid": "ReportAgent-Main",
  "version": 1
}
