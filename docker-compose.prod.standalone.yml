# Standalone: no SMDG Docker network. API/Grafana on host ports for edge proxy.
# Edge (smdg-nginx) reaches via http://172.17.0.1:8000 — not container DNS names.

services:
  fastapi:
    ports:
      - "8000:8000"

  grafana:
    ports:
      - "3000:3000"
